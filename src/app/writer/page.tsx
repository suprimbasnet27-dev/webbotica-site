import MessageCreator from '../../../components/messaging'; // adjust path if needed

export default function WriterPage() {
  return (
    <div className="min-h-screen bg-gray-50 flex justify-center items-start p-6">
      <MessageCreator />
    </div>
  );
}
